import React from "react";

const BacktoTop = () => {
  return <div style={styles.mainContainer}>Back to Top ^</div>;
};

const styles = {
  mainContainer: {
    display: "flex",
    margin: "60px 205px 40px",
    justifyContent: "center",
    alignItems: "center",
  },
};

export default BacktoTop;
